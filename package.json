{
  "name": "jascpal",
  "description": "Paint Shop Pro Palette file reader/writer",
  "version": "0.1.4",
  "license": "MIT",
  "bugs": "https://github.com/goto-bus-stop/jascpal",
  "author": "goto-bus-stop <rene@kooi.me>",
  "files": [
    "/index.js"
  ],
  "devDependencies": {
    "buble": "^0.19.6",
    "standard": "^12.0.1",
    "tap": "^13.1.8"
  },
  "scripts": {
    "prepublish": "npm run build",
    "build": "buble src/Palette.js > index.js",
    "test": "npm run build && standard && tap test/*.test.js",
    "test-cov": "npm run build && standard && tap --100 test/*.test.js"
  },
  "repository": {
    "type": "git",
    "url": "http://github.com/goto-bus-stop/jascpal.git"
  },
  "dependencies": {
    "simple-assign": "^0.1.0"
  }
}
